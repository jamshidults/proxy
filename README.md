# Instructions :
```
Install python-3.7.3.exe
Add Path to True while installing
open CMD Terminal, change directory to proxy folder
Type the command: pip install -r requirements.txt
open the File PRINTER_NAME.py in any file editor and put the printer name inside the quotes without trailing spaces
Double click on the start.bat
```
